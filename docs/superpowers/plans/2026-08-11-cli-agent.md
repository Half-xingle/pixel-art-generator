# CLI Agent 化改造 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 CLI 改造成 agent 可迭代的工具：PNG↔网格 JSON 回环、终端预览、stdin 输入、NES 调色板量化、`--json` 输出契约。

**Architecture:** 恢复 `src/core/palette.js`（预解析优化版量化），新增 `src/cli/preview.js`（ANSI/hex 文本预览），扩展 `cli.js` 为三个命令（draw/convert/pixels）统一入口，全部功能是纯函数或薄 CLI 包装，零新依赖。

**Tech Stack:** Node.js（node:test 测试、spawnSync 集成测试）、sharp（图片 I/O）。

**Spec:** `docs/superpowers/specs/2026-08-11-cli-agent-design.md`

## Global Constraints

- 零新依赖：测试与实现只用现有 node:test + sharp
- 退出码不变：0=成功, 1=参数/格式错误, 2=文件错误
- `--grid`/`--file`/`convert` 老用法全部保留；`OK: ...` 输出格式保留（现有测试断言 `stdout.includes('OK')`）
- `--json` 时 stdout 必须是纯 JSON；预览与错误信息走 stderr
- `--preview` 颜色模式只由 `NO_COLOR` 环境变量（存在即禁用）或 `--no-color` 决定，不做 TTY 检测
- `pixels` 无 `-o` 时 stdout 就是网格 JSON payload，不受 `--json` 影响；`--json` 此时仅在 stderr 附加摘要
- 工作区包含 Task 0 之前的未提交重构改动（简化 pass）：**只 stage 每个任务自己触碰的文件**，Task 0 先统一提交基线
- 每个 commit 末尾加 `Co-Authored-By: Claude <noreply@anthropic.com>`
- 测试运行：单文件 `node --test <file>`，全量 `npm test`

---

### Task 0: 基线提交（现有重构）

**Files:**
- Stage: `README.md`, `cli.js`, `main.js`, `src/cli/image.js`, `src/core/processor.js`, `src/gui/canvas.js`, `src/gui/export.js`, 删除项 `src/core/palette.js`, `src/core/palette.test.js`

**Interfaces:**
- Consumes: 无
- Produces: 干净的基线工作区（palette.js 删除后由 Task 1 以新实现重新创建）

- [ ] **Step 1: 确认测试通过**

Run: `npm test`
Expected: 11 tests pass, 0 fail

- [ ] **Step 2: 提交基线**

先 `git status --short` 确认改动只有 README.md、cli.js、main.js、src/cli/image.js、src/core/processor.js、src/gui/canvas.js、src/gui/export.js（M）、src/core/palette.js、src/core/palette.test.js（D）这 9 个文件（`.superpowers/` 下的改动与 `docs/superpowers/plans/` 未跟踪文件不属于本任务，不 stage）。然后显式路径提交：

```bash
git add README.md cli.js main.js src/cli/image.js src/core/processor.js src/gui/canvas.js src/gui/export.js src/core/palette.js src/core/palette.test.js
git commit -m "refactor: simplify pass — dedupe ratio logic, row-buffer renderGrid, dead code removal

Co-Authored-By: Claude <noreply@anthropic.com>"
```

- [ ] **Step 3: 清理演示产物**

```bash
rm -f cli-demo-16color.png cli-demo-cat-16.png cli-demo-cat-64.png
```

---

### Task 1: 恢复 palette.js（预解析 + quantizeGrid）

**Files:**
- Create: `src/core/palette.js`
- Create: `src/core/palette.test.js`
- Test: `src/core/palette.test.js`

**Interfaces:**
- Consumes: 无
- Produces: `DEFAULT_PALETTE`（16 个 hex 字符串数组）、`quantizeColor(r, g, b, a, palette?) → {r,g,b,a}`、`quantizeGrid(grid, palette?) → grid`（Task 4/5 使用）

- [ ] **Step 1: 写失败测试**

创建 `src/core/palette.test.js`：

```js
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { quantizeColor, quantizeGrid, DEFAULT_PALETTE } from './palette.js';

describe('palette', () => {
  it('provides default palette with 16 colors', () => {
    assert.equal(DEFAULT_PALETTE.length, 16);
    assert.ok(DEFAULT_PALETTE[0].startsWith('#'));
  });

  it('quantizeColor finds exact match', () => {
    assert.deepEqual(quantizeColor(255, 0, 0, 255), { r: 255, g: 0, b: 0, a: 255 });
  });

  it('quantizeColor finds nearest color', () => {
    assert.deepEqual(quantizeColor(200, 10, 10, 255), { r: 255, g: 0, b: 0, a: 255 });
  });

  it('quantizeColor keeps transparent pixels transparent', () => {
    assert.deepEqual(quantizeColor(255, 0, 0, 0), { r: 0, g: 0, b: 0, a: 0 });
  });

  it('quantizeGrid maps every cell onto the palette', () => {
    const grid = [
      [{ r: 12, g: 34, b: 56, a: 255 }, { r: 200, g: 200, b: 200, a: 255 }],
      [{ r: 1, g: 2, b: 3, a: 255 }, { r: 99, g: 88, b: 77, a: 255 }],
    ];
    const out = quantizeGrid(grid);
    const colors = new Set(out.flat().map(c => `${c.r},${c.g},${c.b}`));
    assert.ok(colors.size <= 16);
    assert.ok(out.every(row => row.every(c => c.a === 255)));
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/core/palette.test.js`
Expected: FAIL（模块 `./palette.js` 不存在）

- [ ] **Step 3: 实现**

创建 `src/core/palette.js`：

```js
// Classic NES-inspired 16-color palette
export const DEFAULT_PALETTE = [
  '#000000', '#ffffff', '#888888', '#aaaaaa',
  '#ff0000', '#ff8800', '#ffff00', '#88ff00',
  '#00ff00', '#00ff88', '#00ffff', '#0088ff',
  '#0000ff', '#8800ff', '#ff00ff', '#ff0088',
];

function parseHex(hex) {
  const value = parseInt(hex.slice(1), 16);
  return { r: (value >> 16) & 0xff, g: (value >> 8) & 0xff, b: value & 0xff };
}

// Pre-parsed once at module load — quantize runs per pixel
const DEFAULT_RGB = DEFAULT_PALETTE.map(parseHex);

function colorDistance(r1, g1, b1, r2, g2, b2) {
  const dr = r1 - r2;
  const dg = g1 - g2;
  const db = b1 - b2;
  return dr * dr + dg * dg + db * db;
}

/** Map an RGBA color to the nearest palette color. Transparent stays transparent. */
export function quantizeColor(r, g, b, a, palette = DEFAULT_PALETTE) {
  if (a === 0) return { r: 0, g: 0, b: 0, a: 0 };
  const colors = palette === DEFAULT_PALETTE ? DEFAULT_RGB : palette.map(parseHex);
  let best = null;
  let bestDist = Infinity;
  for (const c of colors) {
    const dist = colorDistance(r, g, b, c.r, c.g, c.b);
    if (dist < bestDist) {
      bestDist = dist;
      best = { r: c.r, g: c.g, b: c.b, a: 255 };
    }
  }
  return best;
}

/** Map every cell of a grid onto the palette. */
export function quantizeGrid(grid, palette = DEFAULT_PALETTE) {
  return grid.map(row => row.map(({ r, g, b, a }) => quantizeColor(r, g, b, a, palette)));
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/core/palette.test.js`
Expected: 5 tests PASS

- [ ] **Step 5: 提交**

```bash
git add src/core/palette.js src/core/palette.test.js
git commit -m "feat: restore NES palette with pre-parsed quantize and quantizeGrid

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: preview.js（终端文本预览）

**Files:**
- Create: `src/cli/preview.js`
- Create: `src/cli/preview.test.js`
- Test: `src/cli/preview.test.js`

**Interfaces:**
- Consumes: `rgbaToHex` from `../core/processor.js`
- Produces: `gridToText(grid, {color = true} = {}) → string`（Task 7 使用）

- [ ] **Step 1: 写失败测试**

创建 `src/cli/preview.test.js`：

```js
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { gridToText } from './preview.js';

const grid = [
  [{ r: 255, g: 0, b: 0, a: 255 }, { r: 0, g: 255, b: 0, a: 0 }],
  [{ r: 0, g: 0, b: 255, a: 255 }],
];

describe('gridToText', () => {
  it('renders ANSI color blocks by default', () => {
    const out = gridToText(grid);
    assert.ok(out.includes('\x1b[48;2;255;0;0m'), 'red block escape');
    assert.ok(!out.includes('\x1b[48;2;0;255;0m'), 'transparent cell has no fill');
    assert.equal(out.split('\n').length, 2);
  });

  it('renders hex grid when color is disabled', () => {
    const out = gridToText(grid, { color: false });
    assert.ok(out.includes('#ff0000'));
    assert.ok(!out.includes('\x1b['));
    assert.ok(out.includes('#00ff0000'), 'transparent green cell shows 8-char hex (rgbaToHex keeps the r/g/b, only adds alpha)');
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/cli/preview.test.js`
Expected: FAIL（`./preview.js` 不存在）

- [ ] **Step 3: 实现**

创建 `src/cli/preview.js`：

```js
import { rgbaToHex } from '../core/processor.js';

/**
 * Render a pixel grid as terminal text.
 * color=true: ANSI truecolor blocks (transparent cells render as blank).
 * color=false: one #rrggbb hex per cell, space-separated (agent-parseable).
 * @param {{r:number,g:number,b:number,a:number}[][]} grid
 * @param {{color?: boolean}} [opts]
 * @returns {string}
 */
export function gridToText(grid, { color = true } = {}) {
  if (color) {
    return grid.map(row =>
      row.map(({ r, g, b, a }) =>
        a < 128 ? '  ' : `\x1b[48;2;${r};${g};${b}m  \x1b[0m`
      ).join('')
    ).join('\n');
  }
  return grid.map(row => row.map(rgbaToHex).join(' ')).join('\n');
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/cli/preview.test.js`
Expected: 2 tests PASS

- [ ] **Step 5: 提交**

```bash
git add src/cli/preview.js src/cli/preview.test.js
git commit -m "feat: terminal grid preview (ANSI blocks / hex grid)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: draw 支持 stdin 输入（`-`）

**Files:**
- Modify: `cli.js`（parseArgs 的 `input` 字段、新增 `readStdin()`、main 的 draw 分支）
- Modify: `src/cli/cli.test.js`（新增 stdin 测试）
- Test: `src/cli/cli.test.js`

**Interfaces:**
- Consumes: 无新依赖
- Produces: `readStdin() → Promise<string>`、`opts.input` 对 draw 含义为 stdin 标记 `'-'`（Task 5 的 round-trip 使用）

- [ ] **Step 1: 写失败测试**

在 `src/cli/cli.test.js` 末尾追加：

```js
describe('cli draw stdin', () => {
  const outFile = join(__dirname, '..', 'test-stdin-output.png');

  after(() => {
    try { unlinkSync(outFile); } catch {}
  });

  it('reads grid JSON from stdin with -', () => {
    const grid = JSON.stringify({
      width: 2, height: 2,
      pixels: [['#ff0000', '#00ff00'], ['#0000ff', '#ffffff']],
    });
    const result = spawnSync(process.execPath, [CLI, 'draw', '-', '--size', '2', '-o', outFile], { input: grid, encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    assert.ok(existsSync(outFile));
  });

  it('accepts a large 64x64 grid via stdin', () => {
    const rows = [];
    for (let y = 0; y < 64; y++) {
      rows.push(Array.from({ length: 64 }, (_, x) => ((x + y) % 2 ? '#ff0000' : '#0000ff')));
    }
    const grid = JSON.stringify({ width: 64, height: 64, pixels: rows });
    assert.ok(grid.length > 30000, `stdin payload is ${grid.length} chars, should exceed 32k arg limit argument`);
    const result = spawnSync(process.execPath, [CLI, 'draw', '-', '--size', '2', '-o', outFile], { input: grid, encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/cli/cli.test.js`
Expected: 新测试 FAIL（stdin 内容被当未知命令 / `-` 无处理）

- [ ] **Step 3: 实现**

在 `cli.js` 修改两处。

(1) parseArgs 的 `input` 字段改为对所有命令取 `args[1]`：

```js
  return {
    command: cmd,
    size: parseInt(getValue('--size') || '16', 10),
    grid: getValue('--grid'),
    file: getValue('--file'),
    output: getValue('-o') || getValue('--output') || 'output.png',
    input: args[1],
  };
```

(2) 新增 `readStdin`（放在 `parseArgs` 之后）并改造 main 的 draw 分支。draw 分支原代码：

```js
  if (command === 'draw') {
    let gridData;
    if (grid) {
      try {
        gridData = JSON.parse(grid);
      } catch {
        console.error('Error: JSON 格式无效');
        process.exit(1);
      }
    } else if (file) {
      let content, json;
      try {
        content = readFileSync(file, 'utf-8');
      } catch {
        console.error(`Error: 无法读取文件 — ${file}`);
        process.exit(2);
      }
      try {
        json = JSON.parse(content);
      } catch {
        console.error('Error: JSON 文件格式无效');
        process.exit(1);
      }
      gridData = json; // Pass entire JSON object (includes width/height/pixels)
    } else {
      console.error('Error: use --grid <json> or --file <path>');
      process.exit(1);
    }
    await cmdDraw(gridData, output, size);
```

替换为：

```js
  if (command === 'draw') {
    let gridData;
    if (grid) {
      try {
        gridData = JSON.parse(grid);
      } catch {
        console.error('Error: JSON 格式无效');
        process.exit(1);
      }
    } else if (file) {
      let content, json;
      try {
        content = readFileSync(file, 'utf-8');
      } catch {
        console.error(`Error: 无法读取文件 — ${file}`);
        process.exit(2);
      }
      try {
        json = JSON.parse(content);
      } catch {
        console.error('Error: JSON 文件格式无效');
        process.exit(1);
      }
      gridData = json; // Pass entire JSON object (includes width/height/pixels)
    } else if (input === '-') {
      let content, json;
      try {
        content = await readStdin();
      } catch {
        console.error('Error: 无法读取 stdin');
        process.exit(2);
      }
      try {
        json = JSON.parse(content);
      } catch {
        console.error('Error: stdin JSON 格式无效');
        process.exit(1);
      }
      gridData = json;
    } else {
      console.error('Error: use --grid <json>, --file <path>, or - (stdin)');
      process.exit(1);
    }
    await cmdDraw(gridData, output, size);
```

在文件顶部 import 区之后（`const CELL_SIZE = 32;` 附近）新增：

```js
/** Read all of stdin as UTF-8 text. */
async function readStdin() {
  const chunks = [];
  for await (const chunk of process.stdin) chunks.push(chunk);
  return Buffer.concat(chunks).toString('utf-8');
}
```

main 的签名改为 `async function main() { const { command, input, size, grid, file, output } = parseArgs(process.argv);` 保持不变（`input` 现在对所有命令都有值）。

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/cli/cli.test.js`
Expected: 全部 PASS（含 2 个新 stdin 测试）

- [ ] **Step 5: 提交**

```bash
git add cli.js src/cli/cli.test.js
git commit -m "feat: draw reads grid JSON from stdin with -

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: convert 支持 --palette nes（+ 共享 computeGridDims）

**Files:**
- Modify: `cli.js`（imports、`computeGridDims`、`resolvePalette`、`cmdConvert`）
- Create: `src/cli/cli-agent.test.js`（调色板测试）
- Test: `src/cli/cli-agent.test.js`

**Interfaces:**
- Consumes: `quantizeGrid` from `./src/core/palette.js`
- Produces: `computeGridDims(width, height, maxSize) → {gridWidth, gridHeight}`（Task 5 的 pixels 复用）、`resolvePalette(name) → DEFAULT_PALETTE | null`（未知名 exit 1）、`cmdConvert(imagePath, maxSize, palette, outputPath)` 新签名

- [ ] **Step 1: 写失败测试**

创建 `src/cli/cli-agent.test.js`：

```js
import { describe, it, before, after } from 'node:test';
import assert from 'node:assert/strict';
import { spawnSync } from 'node:child_process';
import { existsSync, unlinkSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const CLI = join(__dirname, '..', '..', 'cli.js');

describe('cli palette', () => {
  const outFile = join(__dirname, '..', 'test-palette-output.png');
  const testInput = join(__dirname, '..', 'test-palette-input.png');

  before(async () => {
    const { writePNG } = await import('./image.js');
    const { renderGrid } = await import('../core/processor.js');
    // Noisy grid: many near-duplicate colors, far more than 16
    const grid = [];
    for (let y = 0; y < 8; y++) {
      const row = [];
      for (let x = 0; x < 8; x++) {
        row.push({ r: 200 + (x * 7) % 50, g: 30 + (y * 11) % 60, b: 100 + (x + y) % 40, a: 255 });
      }
      grid.push(row);
    }
    const { data, width, height } = renderGrid(grid, 4);
    await writePNG(data, width, height, testInput);
  });

  after(() => {
    try { unlinkSync(outFile); } catch {}
    try { unlinkSync(testInput); } catch {}
  });

  it('quantizes convert output to at most 16 colors', async () => {
    const result = spawnSync(process.execPath, [CLI, 'convert', testInput, '--size', '8', '--palette', 'nes', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    const { default: sharp } = await import('sharp');
    const { data, info } = await sharp(outFile).raw().toBuffer({ resolveWithObject: true });
    const colors = new Set();
    for (let i = 0; i < data.length; i += 4) colors.add(`${data[i]},${data[i + 1]},${data[i + 2]}`);
    assert.ok(colors.size <= 16, `expected <= 16 colors, got ${colors.size}`);
  });

  it('rejects unknown palettes', () => {
    const result = spawnSync(process.execPath, [CLI, 'convert', testInput, '--palette', 'bogus', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 1);
    assert.ok(result.stderr.includes('未知调色板'));
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/cli/cli-agent.test.js`
Expected: FAIL（`--palette` 未被解析，`未知调色板` 不出现）

- [ ] **Step 3: 实现**

`cli.js` 顶部 import 改为：

```js
import { jsonToGrid, renderGrid, toPixelArt } from './src/core/processor.js';
import { readImage, readImageSize, writePNG } from './src/cli/image.js';
import { quantizeGrid, DEFAULT_PALETTE } from './src/core/palette.js';
import { readFileSync } from 'node:fs';
```

`parseArgs` 的返回对象加入两个字段（`grid`/`file` 之后）：

```js
    palette: getValue('--palette'),
    preview: args.includes('--preview'),
    json: args.includes('--json'),
    noColor: args.includes('--no-color') || process.env.NO_COLOR !== undefined,
```

`cmdConvert` 原实现整体替换为：

```js
/** Grid dimensions preserving aspect ratio; maxSize applies to the longest edge. */
function computeGridDims(width, height, maxSize) {
  const aspect = width / height;
  if (aspect >= 1) {
    return { gridWidth: maxSize, gridHeight: Math.max(1, Math.round(maxSize / aspect)) };
  }
  return { gridWidth: Math.max(1, Math.round(maxSize * aspect)), gridHeight: maxSize };
}

/** Resolve a --palette name to a palette array; unknown names exit(1). */
function resolvePalette(name) {
  if (!name) return null;
  if (name === 'nes') return DEFAULT_PALETTE;
  console.error(`Error: 未知调色板: ${name}（支持: nes）`);
  process.exit(1);
}

async function cmdConvert(imagePath, maxSize, palette, outputPath) {
  const { width, height } = await readImageSize(imagePath);
  const { gridWidth, gridHeight } = computeGridDims(width, height, maxSize);
  // Decode already downscaled to the grid — avoids decoding the full image
  const { data, width: decodedW, height: decodedH } = await readImage(imagePath, gridWidth, gridHeight);
  let grid = toPixelArt(data, decodedW, decodedH, gridWidth, gridHeight);
  if (palette) grid = quantizeGrid(grid, palette);
  const result = renderGrid(grid, CELL_SIZE);
  await writePNG(result.data, result.width, result.height, outputPath);
  console.log(`OK: pixel art saved to ${outputPath} (${gridWidth}×${gridHeight})`);
}
```

main 的解构行与 convert 分支调用改为（`palette` 必须解构出来，否则 `--palette` 永远拿不到）：

```js
async function main() {
  const { command, input, size, grid, file, output, palette } = parseArgs(process.argv);
```

```js
    await cmdConvert(input, size, resolvePalette(palette), output);
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/cli/cli-agent.test.js`
Expected: 2 tests PASS

- [ ] **Step 5: 提交**

```bash
git add cli.js src/cli/cli-agent.test.js
git commit -m "feat: convert --palette nes with shared computeGridDims

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: pixels 命令（图片 → 网格 JSON）

**Files:**
- Modify: `cli.js`（imports 加 `writeFileSync`/`gridToJSON`、`cmdPixels`、main 分支、parseArgs 的 `output` 默认值）
- Modify: `src/cli/cli-agent.test.js`（pixels + round-trip 测试）
- Test: `src/cli/cli-agent.test.js`

**Interfaces:**
- Consumes: `computeGridDims`/`resolvePalette`（Task 4）、`readImage`/`readImageSize`/`writePNG`、`toPixelArt`/`gridToJSON`
- Produces: `cmdPixels(imagePath, maxSize, palette, outputPath, json)`（无 outputPath 时 stdout 即网格 JSON）

- [ ] **Step 1: 写失败测试**

先把 `src/cli/cli-agent.test.js` 顶部的 import 行改为（新增 `readFileSync`）：

```js
import { existsSync, readFileSync, unlinkSync } from 'node:fs';
```

然后在文件末尾追加：

```js
describe('cli pixels round-trip', () => {
  const gridFile = join(__dirname, '..', 'test-grid.json');
  const gridFile2 = join(__dirname, '..', 'test-grid2.json');
  const pngFile = join(__dirname, '..', 'test-roundtrip.png');
  const pngOut = join(__dirname, '..', 'test-roundtrip-out.png');
  const testInput = join(__dirname, '..', 'test-roundtrip-input.png');

  before(async () => {
    const { writePNG } = await import('./image.js');
    const { renderGrid } = await import('../core/processor.js');
    const grid = [
      [{ r: 255, g: 0, b: 0, a: 255 }, { r: 0, g: 255, b: 0, a: 255 }],
      [{ r: 0, g: 0, b: 255, a: 255 }, { r: 255, g: 255, b: 255, a: 255 }],
    ];
    const { data, width, height } = renderGrid(grid, 8);
    await writePNG(data, width, height, testInput);
  });

  after(() => {
    for (const f of [gridFile, gridFile2, pngFile, pngOut, testInput]) {
      try { unlinkSync(f); } catch {}
    }
  });

  it('outputs grid JSON to stdout by default', () => {
    const result = spawnSync(process.execPath, [CLI, 'pixels', testInput, '--size', '2'], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    const json = JSON.parse(result.stdout);
    assert.equal(json.width, 2);
    assert.equal(json.height, 2);
    assert.equal(json.pixels[0][0], '#ff0000');
    assert.equal(json.pixels[1][1], '#ffffff');
  });

  it('writes grid JSON to a file with -o', () => {
    const result = spawnSync(process.execPath, [CLI, 'pixels', testInput, '--size', '2', '-o', gridFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    assert.ok(result.stdout.includes('OK'));
    assert.equal(JSON.parse(readFileSync(gridFile, 'utf-8')).pixels[0][0], '#ff0000');
  });

  it('round-trips: pixels → draw → pixels keeps the grid identical', () => {
    spawnSync(process.execPath, [CLI, 'pixels', testInput, '--size', '2', '-o', gridFile], { encoding: 'utf-8' });
    const draw = spawnSync(process.execPath, [CLI, 'draw', '--file', gridFile, '--size', '8', '-o', pngOut], { encoding: 'utf-8' });
    assert.strictEqual(draw.status, 0, `stderr: ${draw.stderr}`);
    const back = spawnSync(process.execPath, [CLI, 'pixels', pngOut, '--size', '2', '-o', gridFile2], { encoding: 'utf-8' });
    assert.strictEqual(back.status, 0, `stderr: ${back.stderr}`);
    const a = JSON.parse(readFileSync(gridFile, 'utf-8'));
    const b = JSON.parse(readFileSync(gridFile2, 'utf-8'));
    assert.deepEqual(a, b);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/cli/cli-agent.test.js`
Expected: 新测试 FAIL（`pixels` 是未知命令）

- [ ] **Step 3: 实现**

`cli.js` 的 import 改为：

```js
import { readFileSync, writeFileSync } from 'node:fs';
import { jsonToGrid, renderGrid, toPixelArt, gridToJSON } from './src/core/processor.js';
```

`parseArgs` 的 `output` 默认值从 `'output.png'` 改为 `null`：

```js
    output: getValue('-o') || getValue('--output') || null,
```

（draw/convert 内部用 `output || 'output.png'` 兜底，见下。）

新增 `cmdPixels`（放在 `cmdConvert` 之后）：

```js
async function cmdPixels(imagePath, maxSize, palette, outputPath, json) {
  const { width, height } = await readImageSize(imagePath);
  const { gridWidth, gridHeight } = computeGridDims(width, height, maxSize);
  const { data, width: decodedW, height: decodedH } = await readImage(imagePath, gridWidth, gridHeight);
  let grid = toPixelArt(data, decodedW, decodedH, gridWidth, gridHeight);
  if (palette) grid = quantizeGrid(grid, palette);
  const payload = JSON.stringify(gridToJSON(grid));
  if (outputPath) {
    writeFileSync(outputPath, payload, 'utf-8');
    console.log(`OK: grid saved to ${outputPath} (${gridWidth}×${gridHeight})`);
  } else {
    // stdout is the payload itself — nothing else may be printed
    process.stdout.write(payload + '\n');
  }
}
```

main 的解构行加 `json`（pixels 分支需要它，否则 ReferenceError），draw/convert 分支调用处补默认值，并新增 pixels 分支。main 解构行：

```js
async function main() {
  const { command, input, size, grid, file, output, palette, json } = parseArgs(process.argv);
```

draw 分支：

```js
    await cmdDraw(gridData, output || 'output.png', size);
```

convert 分支：

```js
    await cmdConvert(input, size, resolvePalette(palette), output || 'output.png');
```

新增分支（放在 convert 分支之后）：

```js
  } else if (command === 'pixels') {
    if (!input) {
      console.error('Error: provide an image path');
      console.error('Usage: node cli.js pixels <image> [--size <n>] [-o <file>]');
      process.exit(1);
    }
    await cmdPixels(input, size, resolvePalette(palette), output, json);
  }
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/cli/cli-agent.test.js`
Expected: 5 tests PASS

- [ ] **Step 5: 提交**

```bash
git add cli.js src/cli/cli-agent.test.js
git commit -m "feat: pixels command — image to grid JSON (stdout or -o)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: --json 输出契约

**Files:**
- Modify: `cli.js`（`opts` 提升到模块级、新增 `fail`/`countColors`、main 与 catch 改造、各命令结果输出）
- Modify: `src/cli/cli-agent.test.js`（json 成功/错误测试）
- Test: `src/cli/cli-agent.test.js`

**Interfaces:**
- Consumes: `rgbaToHex` from `./src/core/processor.js`
- Produces: 模块级 `opts`（Task 7 复用）、`fail(message, code)`、`countColors(grid) → number`

- [ ] **Step 1: 写失败测试**

在 `src/cli/cli-agent.test.js` 末尾追加：

```js
describe('cli --json', () => {
  const outFile = join(__dirname, '..', 'test-json-output.png');

  after(() => {
    try { unlinkSync(outFile); } catch {}
  });

  it('draw --json prints a parseable result object', () => {
    const grid = '[["#ff0000","#00ff00"],["#0000ff","#ffffff"]]';
    const result = spawnSync(process.execPath, [CLI, 'draw', '--size', '2', '--grid', grid, '--json', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    const json = JSON.parse(result.stdout);
    assert.equal(json.ok, true);
    assert.equal(json.grid.width, 2);
    assert.equal(json.grid.height, 2);
    assert.equal(json.colors, 4);
  });

  it('convert --json prints error JSON on stderr with exit 2 for missing file', () => {
    const result = spawnSync(process.execPath, [CLI, 'convert', join(__dirname, '..', 'no-such-file.png'), '--json', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 2);
    const err = JSON.parse(result.stderr);
    assert.equal(err.ok, false);
    assert.ok(typeof err.error === 'string' && err.error.length > 0);
  });

  it('stdout stays empty of non-JSON on error', () => {
    const result = spawnSync(process.execPath, [CLI, 'draw', '--json', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 1);
    assert.equal(result.stdout, '');
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/cli/cli-agent.test.js`
Expected: 新测试 FAIL（stdout 不是 JSON）

- [ ] **Step 3: 实现**

`cli.js` 顶部：import 加 `rgbaToHex`：

```js
import { jsonToGrid, renderGrid, toPixelArt, gridToJSON, rgbaToHex } from './src/core/processor.js';
```

main 开头改为从模块级取 opts，新增 `fail`/`countColors`，放在 `readStdin` 之后：

```js
const opts = parseArgs(process.argv);

/** Print an error message; JSON mode emits {"ok":false,...} on stderr. */
function fail(message, code) {
  if (opts.json) console.error(JSON.stringify({ ok: false, error: message }));
  else console.error(`Error: ${message}`);
  process.exit(code);
}

/** Distinct color count in a grid. */
function countColors(grid) {
  return new Set(grid.flat().map(rgbaToHex)).size;
}
```

main 开头改为（`opts` 已是模块级）：

```js
async function main() {
  const { command, input, size, grid, file, output, palette, json } = opts;
```

把所有 `console.error('Error: ...'); process.exit(N);` 形式的错误处理替换为 `fail('...', N)`。逐一替换（共 8 处）：

1. draw 分支 `JSON 格式无效` → `fail('JSON 格式无效', 1)`
2. draw 分支 `无法读取文件 — ${file}` → `fail('无法读取文件 — ${file}', 2)`
3. draw 分支 `JSON 文件格式无效` → `fail('JSON 文件格式无效', 1)`
4. draw 分支 `无法读取 stdin` → `fail('无法读取 stdin', 2)`；`stdin JSON 格式无效` → `fail('stdin JSON 格式无效', 1)`
5. draw 分支无输入提示 → `fail('use --grid <json>, --file <path>, or - (stdin)', 1)`
6. convert 分支缺图片路径 → `fail('provide an image path', 1)`
7. pixels 分支缺图片路径 → `fail('provide an image path', 1)`
8. 未知命令 → `fail('Unknown command: ' + command, 1)`

（`resolvePalette` 里的 `console.error('Error: 未知调色板...'); process.exit(1)` 同样替换为 `fail('未知调色板: ' + name + '（支持: nes）', 1)`。）

各命令成功输出：`cmdDraw` 尾部：

```js
  const gw = grid[0].length;
  const gh = grid.length;
  if (opts.json) {
    console.log(JSON.stringify({ ok: true, file: outputPath, grid: { width: gw, height: gh }, colors: countColors(grid) }));
  } else {
    console.log(`OK: pixel art saved to ${outputPath} (${gw}×${gh})`);
  }
```

`cmdConvert` 尾部：

```js
  if (opts.json) {
    console.log(JSON.stringify({ ok: true, file: outputPath, grid: { width: gridWidth, height: gridHeight }, colors: countColors(grid) }));
  } else {
    console.log(`OK: pixel art saved to ${outputPath} (${gridWidth}×${gridHeight})`);
  }
```

`cmdPixels` 的 `-o` 分支尾部：

```js
  if (outputPath) {
    writeFileSync(outputPath, payload, 'utf-8');
    if (opts.json) {
      console.log(JSON.stringify({ ok: true, file: outputPath, grid: { width: gridWidth, height: gridHeight }, colors: countColors(grid) }));
    } else {
      console.log(`OK: grid saved to ${outputPath} (${gridWidth}×${gridHeight})`);
    }
  } else {
    process.stdout.write(payload + '\n'); // stdout is the payload itself
    if (opts.json) {
      console.error(JSON.stringify({ ok: true, file: null, grid: { width: gridWidth, height: gridHeight }, colors: countColors(grid) }));
    }
  }
```

文件末尾的 catch 改为：

```js
main().catch(err => {
  if (err.code === 'ENOENT') {
    fail(`文件不存在 — ${err.path}`, 2);
  }
  if (err.message?.toLowerCase().includes('input file')) {
    fail(`无法读取图片 — ${err.message}`, 2);
  }
  fail(err.message, 1);
});
```

（`opts` 为模块级，catch 可直接使用。）

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/cli/cli-agent.test.js` 然后 `npm test`
Expected: 全部 PASS（含既有 cli.test.js 的 `OK` 断言）

- [ ] **Step 5: 提交**

```bash
git add cli.js src/cli/cli-agent.test.js
git commit -m "feat: --json output contract for all commands

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: --preview 终端预览

**Files:**
- Modify: `cli.js`（`printPreview`、draw/convert/pixels 三处调用）
- Modify: `src/cli/cli-agent.test.js`（preview 测试）
- Test: `src/cli/cli-agent.test.js`

**Interfaces:**
- Consumes: `gridToText` from `./src/cli/preview.js`（Task 2）
- Produces: `printPreview(grid)`（内部用模块级 `opts`；`--json` 时预览走 stderr）

- [ ] **Step 1: 写失败测试**

在 `src/cli/cli-agent.test.js` 末尾追加：

```js
describe('cli --preview', () => {
  const outFile = join(__dirname, '..', 'test-preview-output.png');
  const grid = '[["#ff0000","#00ff00"],["#0000ff","#ffffff"]]';

  after(() => {
    try { unlinkSync(outFile); } catch {}
  });

  it('draw --preview prints ANSI blocks', () => {
    const result = spawnSync(process.execPath, [CLI, 'draw', '--size', '2', '--grid', grid, '--preview', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    assert.ok(result.stdout.includes('\x1b[48;2;255;0;0m'));
  });

  it('draw --preview --no-color prints a hex grid', () => {
    const result = spawnSync(process.execPath, [CLI, 'draw', '--size', '2', '--grid', grid, '--preview', '--no-color', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    assert.ok(result.stdout.includes('#ff0000'));
    assert.ok(!result.stdout.includes('\x1b['));
  });

  it('draw --preview --json keeps stdout pure JSON', () => {
    const result = spawnSync(process.execPath, [CLI, 'draw', '--size', '2', '--grid', grid, '--preview', '--json', '-o', outFile], { encoding: 'utf-8' });
    assert.strictEqual(result.status, 0, `stderr: ${result.stderr}`);
    const json = JSON.parse(result.stdout);
    assert.equal(json.ok, true);
    assert.ok(result.stderr.includes('\x1b[48;2;255;0;0m'));
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test src/cli/cli-agent.test.js`
Expected: 新测试 FAIL（`--preview` 被忽略）

- [ ] **Step 3: 实现**

`cli.js` 顶部 import 加 preview：

```js
import { gridToText } from './src/cli/preview.js';
```

在 `countColors` 之后新增：

```js
/** Print the grid as terminal text; JSON mode routes it to stderr to keep stdout pure. */
function printPreview(grid) {
  if (!opts.preview) return;
  const text = gridToText(grid, { color: !opts.noColor });
  if (opts.json) console.error(text);
  else console.log(text);
}
```

三处调用（在各自 renderGrid 之前、拿到 grid 之后）：
- `cmdDraw`：`printPreview(grid);`（`jsonToGrid` 之后）
- `cmdConvert`：`printPreview(grid);`（`quantizeGrid` 之后）
- `cmdPixels`：`printPreview(grid);`（`quantizeGrid` 之后，`payload` 之前）

- [ ] **Step 4: 运行确认通过**

Run: `node --test src/cli/cli-agent.test.js` 然后 `npm test`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add cli.js src/cli/cli-agent.test.js
git commit -m "feat: --preview terminal preview (ANSI blocks, --no-color hex)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 帮助文本 + README + 全量冒烟

**Files:**
- Modify: `cli.js`（`showHelp`）
- Modify: `README.md`（Agent 使用章节）
- Test: 手动冒烟

**Interfaces:**
- Consumes: 全部既有接口
- Produces: 无

- [ ] **Step 1: 更新帮助文本**

`showHelp()` 整体替换为：

```js
function showHelp() {
  console.log(`
🎨 像素画生成器 CLI — Pixel Art Generator

用法:
  node cli.js draw [--grid <json> | --file <path> | -] [--size <n>] -o <file> [--preview] [--json] [--no-color]
  node cli.js convert <image> [--size <n>] -o <file> [--palette nes] [--preview] [--json] [--no-color]
  node cli.js pixels <image> [--size <n>] [-o <file>] [--palette nes] [--preview] [--json] [--no-color]
  node cli.js --help

命令:
  draw      网格 JSON → 像素画 PNG（- 表示从 stdin 读网格）
  convert   图片 → 像素画 PNG（自动保留宽高比）
  pixels    图片 → 网格 JSON（-o 缺省时输出到 stdout，供读取与修改）

选项:
  --size <n>     draw: 每个像素的显示大小（默认 16）
                 convert/pixels: 最长边的网格数（默认 16），另一侧自动按比例计算
  --grid <json>  颜色二维数组 JSON
  --file <path>  JSON 文件路径（含 width/height/pixels 字段）
  -o <file>      输出路径；draw/convert 默认 output.png，pixels 缺省输出到 stdout
  --palette <n>  量化到调色板（支持: nes = 经典 NES 16 色）
  --preview      终端预览网格（ANSI 色块；--no-color 时输出 #rrggbb 网格）
  --json         结果输出为 JSON（错误时 stderr 输出 {"ok":false,...}）
  --no-color     禁用 ANSI 颜色（同 NO_COLOR 环境变量）
  --help         显示帮助信息

示例:
  node cli.js draw --grid '[["#ff0000","#00ff00"],["#0000ff","#ffffff"]]' -o art.png
  node cli.js draw - -o art.png < grid.json
  node cli.js convert photo.jpg --size 32 --palette nes --preview -o pixel-art.png
  node cli.js pixels art.png -o grid.json
`);
}
```

- [ ] **Step 2: 更新 README**

在 README.md 的 `## CLI JSON 格式` 一节之后插入：

```markdown
## Agent 使用

CLI 面向 agent 调用设计，支持完整的"转换 → 修改 → 再渲染"回环：

```bash
# 1. 照片 → 像素画 + 量化 + 预览
node cli.js convert photo.jpg --size 16 --palette nes --preview -o art.png

# 2. 读回网格 JSON（或直接吃 stdout）
node cli.js pixels art.png --size 16 -o grid.json

# 3. 修改后的网格 → 新 PNG
node cli.js draw - -o v2.png --preview < grid.json
```

- `--json`：结果输出结构化 JSON；错误时 stderr 为 `{"ok":false,"error":...}`，退出码 0/1/2
- `--preview`：终端 ANSI 色块预览；`--no-color`（或 `NO_COLOR`）时输出 `#rrggbb` 网格
- `draw -`：从 stdin 读网格 JSON，绕开命令行长度上限（64×64 网格约 30KB）
- `--palette nes`：量化到经典 NES 16 色
```

- [ ] **Step 3: 全量验证**

Run: `npm test`
Expected: 全部 PASS（含之前所有测试）

Run: `node cli.js --help`
Expected: 新帮助文本，含 `pixels`、`--preview`、`--json`、`--palette`

手动冒烟：

```bash
node cli.js convert big-cat.png --size 16 --palette nes --preview --no-color -o smoke-a.png
node cli.js pixels smoke-a.png -o smoke-grid.json
node cli.js draw --file smoke-grid.json --size 8 --preview -o smoke-b.png
```

Expected: 预览输出 hex 网格；三个命令全部 `OK`；`smoke-a.png` 与 `smoke-b.png` 均存在

```bash
rm -f smoke-a.png smoke-b.png smoke-grid.json
```

- [ ] **Step 4: 提交**

```bash
git add cli.js README.md
git commit -m "docs: CLI help and README agent usage section

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review

**Spec 覆盖：**
- palette.js 恢复 + 预解析 ✓ Task 1
- preview.js（ANSI/hex 两种模式）✓ Task 2
- draw stdin ✓ Task 3
- convert/pixels `--palette nes` ✓ Task 4/5
- pixels 命令（stdout payload + `-o`）✓ Task 5
- `--json` 成功/错误契约、stderr 错误 JSON、pixels 无 `-o` 时 stdout 纯 payload ✓ Task 6
- `--preview` + `NO_COLOR`/`--no-color`、`--json` 时预览走 stderr ✓ Task 7
- 退出码 0/1/2 保持 ✓ Task 6（fail 统一）
- 老用法兼容（`--grid`/`--file`/convert）✓ Task 3/4 保留
- 测试：palette ✓ Task 1，回环 ✓ Task 5，preview 双模式 ✓ Task 7，json 成功/错误 ✓ Task 6，stdin 含 64×64 ✓ Task 3
- README agent 用法 ✓ Task 8

**类型/签名一致性：**
- `quantizeGrid(grid, palette?)` Task 4/5 与 Task 1 定义一致
- `gridToText(grid, {color})` Task 7 与 Task 2 定义一致
- `computeGridDims` 返回 `{gridWidth, gridHeight}`，Task 4/5 一致
- `opts` 字段（palette/preview/json/noColor）Task 4 定义、Task 6/7 消费，命名一致
- `cmdConvert(imagePath, maxSize, palette, outputPath)` Task 4 定义、Task 6 结果输出扩展签名一致（同参数）

**占位符检查：** 无 TBD/TODO；所有代码步骤含完整代码。
